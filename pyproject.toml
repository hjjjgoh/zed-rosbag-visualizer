[project]
name = "zed-rosbag-visualizer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.10.6",
    "pyyaml>=6.0.2",
    "ros2-numpy>=0.0.5",
]
